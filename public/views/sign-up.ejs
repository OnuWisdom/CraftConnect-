<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - CraftConnect</title>
    <link rel="stylesheet" href="/css/signup.css" />
</head>
<body>
    <div class="container">
      <div class="image-section">
        <img src="/images/login-page.png" alt="Artisan working" />
      </div>
      <div class="signup-section">
        <div class="signup-header">
          <h1>Turn your passion into purpose start your journey with <span class="stylecraft">Craft</span>Connect.</h1>
          <button class="google-btn">
            <img src="/images/google.png" alt="Google logo" />
            Sign up with Google
          </button>
          <div class="divider">
            <span>Or use Email</span>
          </div>
        </div>

        <form id="signup-form" action="/auth/register" method="POST">
          <div class="signup-input">
            <!-- Full Name - FIXED: Combined into single field -->
            <div>
              <label for="fullname">Full Name</label>
              <input
                type="text"
                id="fullname"
                placeholder="Enter your full name"
                name="fullname"
                required
              />
            </div>
            
            <!-- Username - FIXED: Added missing quote -->
            <div>
              <label for="username">Username</label>
              <input
                type="text"
                id="username"
                placeholder="Create a Username"
                name="username"
                required
              />
            </div>
            
            <!-- Email -->
            <div>
              <label for="email">Email</label>
              <input
                type="email"
                id="email"
                placeholder="Email address"
                name="email"
                required
              />
            </div>
            
            <!-- Password Group - FIXED: Added name attribute to confirm password -->
            <div class="password-group">
              <div class="Eye-toggle">
                <label for="password">Password</label>
                <input
                  type="password"
                  id="password"
                  placeholder="Password"
                  name="password"
                  required
                />
                <span
                  id="toggle-password"
                  class="toggle-password"
                  onclick="togglePassword('password')"
                  >👁️</span
                >
              </div>
              <div class="Eye-toggle">
                <label for="confirmPassword">Confirm Password</label>
                <input
                  type="password"
                  id="confirmPassword"
                  placeholder="Confirm Password"
                  name="confirmPassword"
                  required
                />
                <span
                  id="toggle-confirm-password"
                  class="toggle-password"
                  onclick="togglePassword('confirmPassword')"
                  >👁️</span
                >
              </div>
            </div>
          </div>

          <div class="remember-me">
            <input type="checkbox" name="useUsername" />
            <label>Use username as display name</label>
          </div>

          <button type="submit" class="login-btn">Sign up to CraftConnect</button>

          <div class="signup-footer">
            <p class="note">
              By signing up to CraftConnect, you agree to our
              <a href="/about">Privacy Policy</a> and
              <a href="/about">Terms of Service</a>
            </p>
            <p>
              Already have an account?
              <a href="/login">Login</a>
            </p>
          </div>
        </form>
      </div>
    </div>
    <script src="/js/signup.js"></script>
</body>
</html>